profiles {
    apptainer {
	    apptainer {
	        enabled = true
	        autoMounts = true
	        libraryDir = "$HOME/.apptainer/cache/library/"
            cacheDir = "$HOME/.apptainer/cache/"
			runOptions = '--writable-tmpfs'
			pullTimeout = '1h'
	    }
    }
} 